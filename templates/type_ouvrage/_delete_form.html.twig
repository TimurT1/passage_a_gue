<form method="post" action="{{ path('type_ouvrage_delete', {'id': type_ouvrage.id}) }}" onsubmit={% trans %}"return confirm('Voulez-vous vraiment supprimer cet élément?');"{% endtrans %}>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_ouvrage.id) }}">
    <button class="btn btn-danger">{% trans %}Supprimer{% endtrans %}</button>
</form>
