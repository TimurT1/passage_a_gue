{% extends 'base.html.twig' %}

{% block title %}TypeOuvrage{% endblock %}

{% block main %}
    <h1>TypeOuvrage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_ouvrage.id }}</td>
            </tr>
            <tr>
                <th>NomTypeOuvrage</th>
                <td>{{ type_ouvrage.nomTypeOuvrage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_ouvrage_index') }}">back to list</a>

    <a href="{{ path('type_ouvrage_edit', {'id': type_ouvrage.id}) }}">edit</a>

    {{ include('type_ouvrage/_delete_form.html.twig') }}
{% endblock %}
