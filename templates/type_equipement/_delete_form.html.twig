<form method="post" action="{{ path('type_equipement_delete', {'id': type_equipement.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_equipement.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
