{% extends 'base.html.twig' %}

{% block title %}TypeEquipement{% endblock %}

{% block main %}
    <h1>TypeEquipement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_equipement.id }}</td>
            </tr>
            <tr>
                <th>NomTypeEquipement</th>
                <td>{{ type_equipement.nomTypeEquipement }}</td>
            </tr>
            <tr>
                <th>DescriptionTypeEquipement</th>
                <td>{{ type_equipement.descriptionTypeEquipement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_equipement_index') }}">back to list</a>

    <a href="{{ path('type_equipement_edit', {'id': type_equipement.id}) }}">edit</a>

    {{ include('type_equipement/_delete_form.html.twig') }}
{% endblock %}
