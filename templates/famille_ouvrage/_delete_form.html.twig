<form method="post" action="{{ path('famille_ouvrage_delete', {'id': famille_ouvrage.id}) }}" onsubmit={% trans %}"return confirm('Voulez-vous vraiment supprimer cet élément?');"{% endtrans %}>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ famille_ouvrage.id) }}">
    <button class="btn btn-danger">{% trans %}Supprimer{% endtrans %}</button>
</form>
