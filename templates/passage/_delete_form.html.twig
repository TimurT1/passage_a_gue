<form method="post" action="{{ path('passage_a_gue_delete', {'id': passage_a_gue.id}) }}" onsubmit={% trans %}"return confirm('Voulez-vous vraiment supprimer ce passage?');"{% endtrans %}>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ passage_a_gue.id) }}">
    <button class="btn btn-danger">{% trans %}Supprimer{% endtrans %}</button></div>
</form>
